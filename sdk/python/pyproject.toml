[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "tracemind-sdk"
version = "0.3.0"
description = "TraceMind Python SDK"
requires-python = ">=3.9"
dependencies = ["httpx>=0.24.0"]

[tool.setuptools.packages.find]
where = ["."]